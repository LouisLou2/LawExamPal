class UIFitter{
  static Duration dialogDelay=const Duration(milliseconds: 200);// 为了防止dialog还未成功弹出就被dismiss掉，引发错误
  static Duration pageAnimDuration=const Duration(milliseconds: 400);
  static Duration pageAnimDurationShort=const Duration(milliseconds: 200);
  static Duration pageAnimDurationLong=const Duration(milliseconds: 600);
}